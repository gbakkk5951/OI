#include <bits/stdc++.h>

using namespace std;

const int N = 100010;

typedef long long ll;

int main() {
    /*
        给定的是一棵树，要求连通，所以都要选
    */
    freopen("road0.in", "r", stdin);
    freopen("road0.out", "w", stdout);
    for(int i = 1 ; i <= 99 ; ++ i) printf("%d\n", i);
}